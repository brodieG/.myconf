# # setting CC to gcc just seems to alias back to clang
# CC = gcc
# CFLAGS += -O0 -Wall -std=gnu99

# # C 90
# CFLAGS = -O0 -Wall -std=c99 -pedantic -Wextra -D_ANSI_SOURCE
CFLAGS += -fno-common -std=gnu99 -pedantic -Wall

# # Output assembler in intel syntax
# CFLAGS += -S -masm=intel -O0

# For Valgrind, and for detecting non-compliance with C99

# CFLAGS += -O0 -std=c99 -Wextra -pedantic -Wuninitialized -Wstrict-overflow -D_POSIX_C_SOURCE=200112L
